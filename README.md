# sleep-requirement-calculator
Calculates sleep time requirement with TensorFlow. The model is trained with a dataset that includes: WorkoutTime, ReadingTime, PhoneTime, WorkHours, Caffeine, RelaxationTime, SleepTime.
